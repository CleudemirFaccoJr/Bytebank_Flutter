import 'package:flutter/material.dart';

class EditarTransacaoScreen extends StatelessWidget {
  const EditarTransacaoScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('EditarTransacaoScreen'),
      ),
      body: const Center(
        child: Text('EditarTransacaoScreen'),
      ),
    );
  }
}